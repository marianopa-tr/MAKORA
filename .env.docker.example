# ── Makora Shared App — Docker Environment Variables ─────────────────
# Copy this file:  cp .env.docker.example .env.docker
# Then fill in your values.
# ─────────────────────────────────────────────────────────────────────

# ── LLM Provider (required for signal research) ─────────────────────
# Supported: openai-raw (default), ai-sdk, cloudflare-gateway, azure-openai
LLM_PROVIDER=openai-raw
LLM_MODEL=gpt-4o-mini

# Choose ONE provider key based on LLM_PROVIDER:
OPENAI_API_KEY=sk-your-openai-key

# Azure OpenAI (if LLM_PROVIDER=azure-openai):
# AZURE_API_KEY=
# AZURE_RESOURCE_NAME=
# AZURE_ENDPOINT=

# Anthropic (if LLM_PROVIDER=ai-sdk, LLM_MODEL=anthropic/...):
# ANTHROPIC_API_KEY=

# Google (if LLM_PROVIDER=ai-sdk, LLM_MODEL=google/...):
# GOOGLE_GENERATIVE_AI_API_KEY=

# xAI / Grok (if LLM_PROVIDER=ai-sdk, LLM_MODEL=xai/...):
# XAI_API_KEY=

# DeepSeek (if LLM_PROVIDER=ai-sdk, LLM_MODEL=deepseek/...):
# DEEPSEEK_API_KEY=

# ── Security ────────────────────────────────────────────────────────
# Leave blank to auto-generate on startup.
MAKORA_API_TOKEN=
KILL_SWITCH_SECRET=

# ── Shared signal gathering (optional) ──────────────────────────────
# If you want the main harness to also gather social signals centrally,
# provide your own eToro credentials here. Individual users connecting
# via the UI send their own keys through the browser (never stored).
# ETORO_API_KEY=
# ETORO_USER_KEY=
# ETORO_ENV=demo

# ── Optional ────────────────────────────────────────────────────────
# DEBUG=etoro
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# TWITTER_BEARER_TOKEN=
